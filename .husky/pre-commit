# Load NVM if it exists so GUI clients (like GitHub Desktop) can find Node and Yarn
export NVM_DIR="$HOME/.nvm"
[ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"

# Load local huskyrc if it exists for user-specific PATH overrides
[ -f ~/.huskyrc ] && . ~/.huskyrc

npx lint-staged
yarn lint
yarn test